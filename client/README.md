# 🖥️ HireFlow — Client Frontend Application

The modern web client for HireFlow built with **React 19**, **Vite 7**, **Tailwind CSS v4**, and **Clerk Authentication**.

---

## ⚡ Quick Start

```bash
# Install dependencies
npm install

# Start development server
npm run dev

# Build for production
npm run build
```

## ⚙️ Environment Variables

Create `.env` inside the `client/` directory:

```env
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
VITE_API_BASE_URL=http://localhost:5000/api
```

---

> For full documentation, architecture diagrams, and automation guides, refer to the [Root README.md](../README.md).
